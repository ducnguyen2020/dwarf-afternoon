import React, {Component} from 'react'
import Realm from './Realm';

class RealmText extends React.Component{
    render(){
        return(
            <h2>From around the Realm</h2>
        )
    }
}

export default RealmText